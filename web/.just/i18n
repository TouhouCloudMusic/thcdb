default: sync

sync:
	pnpm exec wuchale --config ../wuchale.config.ts --clean

check:
	pnpm exec wuchale --config ../wuchale.config.ts check

status:
	pnpm exec wuchale --config ../wuchale.config.ts status
