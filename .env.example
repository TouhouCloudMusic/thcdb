ADMIN_PASSWORD=changeme

# Required by server/config.toml email config.
# Resend SMTP host / username / security / port are already fixed there.
EMAIL__CREDS__PASSWORD=

# Optional overrides:
# POSTGRES_USER=devuser
# POSTGRES_PASSWORD=devuser
# POSTGRES_DB=devdb
# EMAIL__FROM=noreply@example.com
# SERVER_PORT=12345
# WEB_PORT=3000
