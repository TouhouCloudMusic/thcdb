[project]
name = "thcdb"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "bashlex>=0.18,<1",
    "httpx>=0.27,<1",
    "python-dotenv>=1.0,<2",
]

[tool.ruff]
target-version = "py311"
